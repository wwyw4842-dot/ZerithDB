# ─── Server ──────────────────────────────────────────────────────────────────
PORT=4000
HOST=0.0.0.0

# ─── OpenTelemetry ────────────────────────────────────────────────────────────
# Service name shown in Datadog / Jaeger traces
OTEL_SERVICE_NAME=zerithdb-signaling

# OTLP HTTP collector endpoint.
# Jaeger  → http://jaeger-host:4318
# Datadog → http://datadog-agent:4318  (requires DD Agent with OTLP receiver enabled)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Set to "true" to disable all telemetry (e.g. in local dev without a collector)
OTEL_SDK_DISABLED=false
